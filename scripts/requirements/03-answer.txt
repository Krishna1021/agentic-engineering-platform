Aggregate successful redirects per short code by UTC calendar day, and expose totalRedirects plus dailyCounts through GET /api/links/{shortCode}/analytics. Do not retain IP addresses, user agents, cookies or individual visitor identifiers. Keep daily aggregate rows indefinitely for this prototype and explicitly document this limitation. Unknown codes return 404 without creating counts. Use atomic increments under concurrency. A failed analytics write must not block a valid redirect; counting is best effort. Preserve the existing creation/redirect contracts, add regression and acceptance tests, and record these choices as explicit assumptions and trade-offs.
